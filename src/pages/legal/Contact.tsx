import { Layout } from "@/components/layout/Layout";
import { LegalPageContent } from "@/components/LegalPageContent";

export default function Contact() {
  return (
    <Layout>
      <LegalPageContent type="contact" />
    </Layout>
  );
}