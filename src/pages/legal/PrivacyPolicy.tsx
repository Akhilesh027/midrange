import { Layout } from "@/components/layout/Layout";
import { LegalPageContent } from "@/components/LegalPageContent";

export default function PrivacyPolicy() {
  return (
    <Layout>
      <LegalPageContent type="privacy_policy" />
    </Layout>
  );
}