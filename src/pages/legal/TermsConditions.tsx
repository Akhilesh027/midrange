import { Layout } from "@/components/layout/Layout";
import { LegalPageContent } from "@/components/LegalPageContent";

export default function TermsConditions() {
  return (
    <Layout>
      <LegalPageContent type="terms_conditions" />
    </Layout>
  );
}